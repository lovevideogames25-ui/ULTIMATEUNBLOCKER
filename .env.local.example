# Created by Vercel CLI - Example Environment Variables
# This file is managed by Vercel CLI and contains only the OIDC token
# All other environment variables should be in your .env file

# Vercel OIDC Token (automatically managed by Vercel CLI)
# This token is required for Vercel AI Gateway authentication
# Run 'vercel env pull' to refresh this token (expires every 12 hours)
VERCEL_OIDC_TOKEN="your-vercel-oidc-token-here"

# Note: API keys and other configuration should be in your .env file
# See .env.example for the complete environment variable structure
