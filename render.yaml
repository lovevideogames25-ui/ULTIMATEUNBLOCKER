services:
  - type: web
    name: ultimateunblocker
    env: node
    buildCommand: echo 'No build command needed'
    startCommand: node simple-server.js
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
